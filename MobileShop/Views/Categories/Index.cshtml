@model IEnumerable<MobileShop.Models.Category>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a class="btn btn-primary" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CatName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CatName)
            </td>
            <td>
                <a class="btn btn-warning" asp-action="Edit" asp-route-id="@item.CatID">Edit</a> |
                <a class="btn btn-info" asp-action="Details" asp-route-id="@item.CatID">Details</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.CatID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
